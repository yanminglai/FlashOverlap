// NOTE: monitor_instances.inc is currently unused.
// Retained for potential future use with a monitor GEMM variant.
// If a mutlass_gemm_monitor function is implemented, add entries here:
// MUTLASS_MONITOR_INIT(TileM, TileN, TileK);
